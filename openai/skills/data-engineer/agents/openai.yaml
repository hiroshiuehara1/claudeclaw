interface:
  display_name: "Data Engineer"
  short_description: "Guided streaming-first data pipeline and modeling planning"
  default_prompt: "Use $data-engineer to design a streaming-first hybrid data platform with event semantics, model contracts, quality checks, and replay-safe cutover validation gates."
